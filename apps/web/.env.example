# 本地开发 mock 配置（不提交到 git）
# 非 iframe 环境自动生效

VITE_API_BASE_URL=http://localhost:3000

# mock 用户信息（对应 iframeMessage.getInfo()）
VITE_MOCK_USER_ID=@dev_user:local
VITE_MOCK_DISPLAY_NAME=Dev Player
VITE_MOCK_POWER_LEVEL=100
VITE_MOCK_ROOM_ID=!dev_room:local

# mock Token（对应 iframeMessage.getToken()）
# 替换成一个真实的 Matrix Bearer Token 用于实际调用 API
VITE_MOCK_TOKEN=syt_a2ltaWdhbWUx_WEnXKgiFtirbEiSPTMwU_2p1YIY
